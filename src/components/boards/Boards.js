import React from "react";
import ProductList from "../product/ProductList";
// import ProductItem from "../product/ProductItem";

function Boards({ productList, cart, addToCart }) {

    return (
        <div>
            <ProductList />
            Boards
        </div>
    )
}
export default Boards; 